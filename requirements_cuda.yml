name: ci_env

channels:
  - anaconda
  - conda-forge
  - grst
  - bioconda
  - nvidia
  - rapidsai
  - pytorch
dependencies:
  - python >= 3.8
  - click
  - h5py
  - vigra
  - scikit-image
  - tqdm
  - pandas
  - seaborn
  - colorcet
  - dill
  - opencv
  - pytest
  - scanpy
  - anndata
  - jupyter
  - jupyterlab
  # torch stuff
  - pytorch == 1.7.0
  - torchvision == 0.8.1
  - torchaudio == 0.7.0
  - cudatoolkit ~= 10.2
  # pip stuff
#  - pip
#  - pip:
#      - cppimport

#mamba install pytorch==1.7.0 torchvision==0.8.1 torchaudio==0.7.0 cudatoolkit=10.2 -c pytorch -y
