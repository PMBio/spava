name: Visium tiler
on:
  workflow_dispatch
jobs:
  test-data:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v2
      - run: |
          source ~/.bashrc
          python notebook_exporter.py datasets/tilers/visium_data_tiler.py ci_env
