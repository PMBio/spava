name: Importing the data
on:
  [workflow_dispatch, push]
jobs:
  test-data:
    runs-on: self-hosted
    steps:
      - run: |
          source ~/.bashrc
          cd /data/l989o/deployed/a
          conda activate ci_env
          python -m tests.test_data_import
#          python /data/l989o/deployed/a/tests/test_data.py
#
